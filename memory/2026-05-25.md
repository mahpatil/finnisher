# 2026-05-25

## Wrap-up: Step 8 — Dashboard PATCH Error Handling
- Shipped: Shared `patchThread` in `src/web/lib/api.ts` (throws on non-2xx); MUI Snackbar error toasts on Dashboard + Threads page; 5 unit + 5 Playwright tests; `dev:network` npm script for LAN access
- Verdict: APPROVED
- Deferred: none
- Next: Step 9 — TBD

## Wrap-up: Step 7 — finn web background + global install fix
- Shipped: Fixed `finn web` binary path for global npm install; proper background detach (stdio→web.log + child.unref()); `finn web status` command; `prepare` script; full README rewrite
- Verdict: APPROVED
- Deferred: Dashboard PATCH silent error handling (next step)
- Next: Step 8 — dashboard PATCH error handling

## Wrap-up: Hotfix — hook parent-repo git remote bleed
- Shipped: `getGithubUrl` guard against parent repo walk-up; `realpathSync` for macOS symlink normalisation; 9 new tests for getGithubUrl / getThreadId / ensureThreadId; fluicrm `.finn-thread` planted
- Verdict: APPROVED (green tests, no reviewer)
- Deferred: none
- Next: Step 8 — dashboard PATCH error handling
